presentJson = (data) => { const name = data.name; 
  const picture = data.avatar_url; 
  const url = data.html_url;
  const stargazers = data.stargazers_count;
  const forks = data.forks;
  const language = data.language;
};